load("//tools:defaults.bzl", "ts_library")

package(default_visibility = ["//visibility:public"])

ts_library(
    name = "saucelabs-bazel",
    srcs = [
        "browser.ts",
        "ipc-defaults.ts",
        "ipc-messages.ts",
    ],
    deps = [],
)
