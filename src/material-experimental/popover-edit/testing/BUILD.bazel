package(default_visibility = ["//visibility:public"])

load("//tools:defaults.bzl", "ng_test_library", "ng_web_test_suite", "ts_library")

ts_library(
    name = "testing",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    module_name = "@angular/material-experimental/popover-edit/testing",
    deps = [
        "//src/cdk/coercion",
        "//src/cdk/testing",
        "//src/material/table/testing",
    ],
)

filegroup(
    name = "source-files",
    srcs = glob(["**/*.ts"]),
)
